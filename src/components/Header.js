import React from 'react'
import Button  from './Button'

const Header = () => {
    return (
        <header>
            <h2>Task Tracker</h2>
            <Button color='green' text='Add'/>
            
        </header>
    )
}

export default Header
